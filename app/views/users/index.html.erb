<h2>Masters</h2>
<table>
    <thead>
        <th>Name</th>
        <th>Location</th>
        <th>Skills</th>
    </thead>
    <tbody>
        <% @users.each do |user| %>
        <tr>
          <td><%= user.name %></td>
          <td><%= user.location %></td>
          <td><%= user.skills.map(&:description).join(", ") %></td>
        </tr>
        <% end %>
    </tbody>
</table>

